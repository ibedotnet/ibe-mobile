const clientOverrides = {
  1331: { forceDisableSwitch: true }, // Switch is always disabled for this client
  // Other clients are not listed, meaning they follow the default behavior
};

export default clientOverrides;