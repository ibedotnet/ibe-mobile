# Run unit tests before commit (uncomment to enable)
# npm test

# Lint and format staged files before commit (uncomment to enable)
#npx lint-staged

# Note:
# These commands are disabled to speed up commit times.
# Uncomment any line above if you want it to run automatically on every commit.